<div class="container">
  <div class="row extra-top-margin">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <h3 class="panel-title"><%= @city %></h3>
      </div>
      <div class="panel-body">
        <% if not @forecasts.empty? %>
          <% @forecasts["list"].each_with_index do |forecast, index| %>
          <div class="col-xs-12 col-sm-4 col-md-3">
            <div>
              <h4><%= forecast["temp"]["day"] %>º</h4>
              <time>
                <a data-toggle="modal"
                  href=<%= "detail-modal-#{index}"%>
                  data-target="<%= "#detail-modal-#{index}"%>">

                  <%= (Time.now + index.day).strftime("%d/%m/%Y") %>
                </a>
              </time>

              <div class="modal fade"
                tabindex="-1" role="dialog"
                aria-labelledby="mySmallModalLabel"
                id="<%= "detail-modal-#{index}"%>">

                <div class="modal-dialog">
                  <div class="modal-content">
                    <div class="modal-header">
                      <button type="button"
                        class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                      </button>
                      <h4 class="modal-title">
                        <%= @city %>
                        <%= (Time.now + index.day).strftime("%d/%m/%Y") %> - <%= forecast["temp"]["day"] %>º
                      </h4>
                    </div>
                    <div class="modal-body">
                      <p>Min: <%= forecast["temp"]["min"] %>º</p>
                      <p>Max: <%= forecast["temp"]["max"] %>º</p>
                      <p>Pressure: <%= forecast["temp"]["pressure"] %></p>
                      <p>Humidity: <%= forecast["temp"]["humidity"] %></p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <% end %>
        <% else %>
          <p>Sorry we didn't find a forecast.</p>
        <% end %>
      </div>
    </div>
  </div>
</div>
